#pragma once


namespace math
{
	double pow2(const int power);
	double max(const double a, const double b);
	int avg(const int* array, const int size);
	int max(const int* array, const int size);
	int min(const int* array, const int size);
}